Run the GPS radius album update script to add any GPS-tagged photos within a configured radius of each city center to the corresponding landmark album:

```bash
python3 user_scripts/update_gps_radius_albums.py
```

Report how many photos were added to each album and the total count added.
