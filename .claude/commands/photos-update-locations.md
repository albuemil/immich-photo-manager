Run the location albums update script to add any missing photos to existing landmark albums (no new albums are created):

```bash
python3 user_scripts/update_location_albums.py
```

Report how many photos were added to each album, and the total count added.
